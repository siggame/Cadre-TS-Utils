/**
 * Validates that a string is a valid url.
 *
 * @param str - The string to try to validate.
 * @returns true if looks like a valid url, false otherwise.
 */
export declare function validateURL(str: string): boolean;
